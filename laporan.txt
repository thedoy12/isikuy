LAPORAN AUDIT DAN PEKERJAAN PROJECT ISIKUY
Tanggal: 19 Mei 2026

Ringkasan:
- Project sudah dicek lewat lint, TypeScript, test, audit dependency, dan build production.
- Ditemukan 2 masalah runtime yang perlu diperbaiki.
- Keduanya sudah diperbaiki di kode lokal.

Checklist pemeriksaan:
- npm run lint: LULUS
- npm run check: LULUS
- npm run test: LULUS
- npm audit --audit-level=high: LULUS, 0 vulnerability
- npm run build: LULUS
- Cek log PM2 VPS: ditemukan error lama terkait Gemini quota dan satu error fallback index.html saat deploy.

Bug / error / kejanggalan yang ditemukan:
1. Error Gemini quota memenuhi log VPS.
   Detail:
   - Log PM2 berisi banyak error 429 RESOURCE_EXHAUSTED dari Gemini.
   - Saat quota Gemini habis, backend mencoba beberapa model dan menulis log panjang berulang.
   Dampak:
   - Log server cepat penuh.
   - Fitur AI tetap fallback ke template, tetapi server terlalu sering mencoba model yang sedang kena limit.
   Perbaikan:
   - Ditambahkan cooldown per model Gemini saat menerima status 429.
   - Log Gemini dibuat lebih ringkas supaya tidak memenuhi PM2 log.
   File:
   - api/routers/tools.ts

2. Fallback React app bisa melempar error ENOENT ketika index.html belum tersedia.
   Detail:
   - PM2 pernah mencatat error:
     ENOENT: no such file or directory, open '/var/www/isikuy/dist/public/index.html'
   - Ini bisa terjadi saat request masuk di momen deploy ketika file build belum siap/terbaca.
   Dampak:
   - User bisa melihat error server sementara.
   - Log server jadi berisi stack trace.
   Perbaikan:
   - Fallback SPA sekarang mengecek keberadaan index.html.
   - Jika file belum siap, server mengembalikan status 503 dengan pesan singkat, bukan stack trace.
   File:
   - api/lib/vite.ts

Temuan yang tidak langsung diubah:
1. Bundle frontend masih besar.
   Detail:
   - Build memberi warning chunk lebih dari 500 kB.
   - Ini bukan error fatal, aplikasi tetap berhasil build.
   Rekomendasi:
   - Nanti bisa dipisah dengan dynamic import / code splitting untuk halaman admin dan halaman besar lain.

2. Gemini quota masih bergantung pada limit akun/API key.
   Detail:
   - Cooldown sudah mengurangi spam request dan spam log.
   - Jika quota API memang habis, hasil AI akan tetap fallback ke template.
   Rekomendasi:
   - Upgrade quota/API billing atau ganti API key jika fitur AI harus selalu aktif.

Pekerjaan yang sudah dikerjakan hari ini:
1. Perbaikan halaman Tools:
   - Memperbaiki tampilan wheel yang sebelumnya berantakan.
   - Membuat menu/mode wheel bisa custom sesuai input pengguna.
   - Menghapus bagian Fake Fun.
   - Mengatur AI hanya dipakai di kategori AI Text dan Fun Viral.
   - Memperbaiki bug calculator yang angka depannya sulit dihapus/diketik.
   - Menghentikan request tracking tools yang sebelumnya berulang ke /api/trpc/tools.track.

2. Perbaikan auth dan akun user:
   - Menambahkan halaman Pengaturan Akun di /account.
   - Menambahkan menu Pengaturan Akun di navbar user.
   - Login sekarang bisa pakai username, email, atau nomor telepon.
   - Register sekarang wajib input nomor telepon.
   - User bisa update nama, email, nomor telepon, avatar, dan password.
   - Admin edit user sekarang bisa edit nomor telepon.
   - Database ditambah kolom phone untuk users dan sudah diterapkan di VPS.

3. Fitur voucher:
   - Mengecek penyebab code voucher tidak berfungsi.
   - Menemukan tabel vouchers di produksi masih kosong.
   - Membuat halaman admin voucher di /admin/vouchers.
   - Admin bisa membuat, mengedit, menghapus, mengaktifkan/nonaktifkan voucher.
   - Voucher mendukung diskon persen atau nominal, minimal order, max diskon, limit penggunaan, dan masa berlaku.
   - Kode voucher disimpan uppercase.
   - Checkout langsung menampilkan potongan di UI saat kode voucher valid.

4. Deploy dan validasi:
   - Perubahan sebelumnya sudah dipush ke GitHub.
   - Perubahan sebelumnya sudah dideploy ke VPS http://202.10.37.162/.
   - PM2 service isikuy sudah direstart dan online.
   - Halaman /account dan /admin/vouchers sudah diverifikasi return HTTP 200.

Perbaikan terbaru setelah audit ini:
1. api/routers/tools.ts
   - Menambahkan cooldown per model Gemini ketika terkena 429 quota.
   - Mengurangi log error Gemini yang terlalu panjang.

2. api/lib/vite.ts
   - Menambahkan guard untuk fallback index.html.
   - Mengembalikan 503 sementara bila index.html belum siap, bukan crash/stack trace.

Status akhir lokal:
- Lint: OK
- TypeScript: OK
- Test: OK
- Audit dependency: OK, 0 vulnerability
- Build production: OK

Catatan:
- File laporan ini dibuat sesuai permintaan user.
- Perbaikan terbaru dilanjutkan untuk dipush dan dideploy ke VPS.

Update 19 Mei 2026 - Perbaikan sistem markup dan harga:

Temuan awal:
1. Sistem harga belum punya status manual/otomatis.
   Detail:
   - Harga modal Flowix disimpan di products.basePrice.
   - Harga jual user disimpan di products.salePrice.
   - Produk baru diberi markup otomatis dari PRODUCT_MARKUP_PERCENT, default 3%.
   - Setelah produk pernah tersync, salePrice lama bisa tertahan sehingga tidak selalu ikut harga Flowix terbaru.

2. Voucher bisa membuat transaksi rugi.
   Detail:
   - Diskon voucher sebelumnya dibatasi hanya sampai nilai harga jual.
   - Belum ada batas bawah berdasarkan modal Flowix.
   - Contoh kasus 3 Diamonds: modal Flowix Rp1.051, harga jual Rp1.100, voucher Rp55 membuat nilai bersih Rp1.045.

3. Admin belum punya UI jelas untuk kontrol harga produk.
   Detail:
   - Backend sudah punya endpoint update produk.
   - Halaman admin Games belum menampilkan tabel modal, harga jual, margin, dan mode harga.

4. Grafik profit admin belum memakai margin produk.
   Detail:
   - Profit sebelumnya dihitung dari feeAmount.
   - Karena feeAmount QRIS disimpan 0, grafik profit tidak mencerminkan margin produk.

Perbaikan yang dikerjakan:
1. Menambahkan helper pricing.
   File:
   - api/lib/pricing.ts
   Isi:
   - priceWithMarkup() untuk hitung harga jual dari modal + markup.
   - parseMoney() untuk parsing numeric database.
   - safeDiscountAmount() untuk membatasi diskon agar tidak melebihi nilai pesanan.

2. Menambahkan status harga manual/otomatis.
   File:
   - db/schema.ts
   Perubahan:
   - Menambahkan kolom products.isPriceManual default false.
   Dampak:
   - Produk auto akan ikut harga Flowix saat sync katalog.
   - Produk manual akan mempertahankan harga jual yang diatur admin.

3. Memperbaiki sync katalog Flowix.
   File:
   - api/routers/game.ts
   Perubahan:
   - Harga jual produk auto dihitung ulang setiap sync dari harga Flowix terbaru.
   - Harga jual produk manual tidak ditimpa oleh sync.
   - Produk baru tetap memakai markup otomatis default.

4. Memperbaiki update produk admin.
   File:
   - api/routers/admin.ts
   Perubahan:
   - updateProduct sekarang bisa menyimpan harga jual manual.
   - updateProduct sekarang bisa reset harga ke mode auto.
   - Query produk admin mengembalikan isPriceManual.

5. Memperbaiki kalkulasi voucher di checkout.
   File:
   - api/routers/payment.ts
   - api/routers/transaction.ts
   Perubahan:
   - Diskon voucher tetap mengikuti setting admin: nominal/persen, minimal order, max diskon, limit penggunaan, dan masa berlaku.
   - Diskon voucher boleh membuat harga di bawah modal untuk kebutuhan promosi.
   - Transaksi final memakai aturan yang sama, jadi validasi frontend dan backend konsisten.

6. Menambahkan kontrol harga produk di admin Games.
   File:
   - src/pages/admin/Games.tsx
   Fitur:
   - Admin bisa melihat modal Flowix/base price.
   - Admin bisa melihat dan mengedit harga jual/sale price.
   - Admin bisa melihat margin per produk.
   - Admin bisa melihat mode AUTO/MANUAL.
   - Admin bisa reset harga produk ke AUTO.
   - Admin bisa ON/OFF produk dari tabel harga.

7. Memperbaiki dasar profit dashboard.
   File:
   - api/routers/admin.ts
   Perubahan:
   - Profit chart 7 hari sekarang dihitung dari nilai bersih transaksi setelah voucher dan modal produk.
   - Ini lebih mendekati margin produk dibanding feeAmount yang sebelumnya bernilai 0.

Status verifikasi lokal:
- Lint: OK
- TypeScript: OK
- Test: OK
- Build production: OK

Catatan lanjutan:
- PRODUCT_MARKUP_PERCENT di VPS masih memakai default 3% jika env tidak diisi.
- Jika nanti ingin markup berbeda per kategori/game, perlu ditambah setting markup global/per kategori di database.

Update koreksi voucher:
- Aturan batas bawah modal untuk voucher dibatalkan sesuai arahan user.
- Voucher sekarang boleh dipakai untuk promosi di bawah modal karena admin bisa mengatur nilai, max diskon, min order, dan limit voucher sendiri.
- Fitur markup AUTO/MANUAL dan kontrol harga produk admin tetap dipertahankan.

Update 19 Mei 2026 - Pagination admin:
- Menambahkan pagination GEMINI_ALERTS di halaman Tools Monitor.
- Menambahkan pagination VOUCHER_LIST di halaman Vouchers.
- Menambahkan pagination RECENT_TRANSACTIONS di Dashboard.
- Menambahkan pagination PRODUCT_PRICING di Admin Games.
- Transactions, Users, dan daftar Games tetap memakai pagination yang sudah ada.
- Verifikasi lokal: lint OK, TypeScript OK, test OK, build OK.

Update 19 Mei 2026 - Login gate Tools:
- Menambahkan publicAccess untuk beberapa tools random yang boleh dimainkan tanpa login: challenge-wheel, punishment-wheel, hero-picker, aura-calculator.
- Tools selain daftar publik sekarang wajib login sebelum generate/spin.
- Menambahkan popup pemberitahuan login/register di halaman detail tool.
- Menambahkan label LOGIN di kartu tools yang terkunci untuk pengunjung.
- Backend generateText sekarang ikut menolak user belum login untuk tools terkunci, jadi tidak bisa dibypass dari UI.
- Verifikasi lokal: lint OK, TypeScript OK, test OK, build OK.
